# מסלול — ייעול משלוחים למסעדות עם שליחים משלהן

מונורפו TypeScript. Supabase (Postgres + RLS + Realtime), React + Vite,
Leaflet. שתי אפליקציות: `apps/dashboard` (בעל עסק), `apps/courier` (שליח).

**רד קודם ל-`spec.md`** (בשורש, לא ב-`docs/`) — שם האפיון המלא, תוכנית
השלבים והנימוקים. כל סעיף שם מסומן ✅ נבנה או 🔜 מתוכנן.
המסמך הזה מכיל רק את מה שאסור לגלות מחדש בדרך הקשה.

---

## האילוץ העסקי שמעצב הכל

אומת מול בתי עסק אמיתיים:

> **שליח שיצא למסלול לא חוזר למסעדה לאסוף משלוח נוסף באמצע.**
> מנה שמוכנה ממתינה עד שהוא חוזר, או נלקחת בידי שליח אחר.

לכן כל יציאה היא **אצווה סגורה**, ואין בעיית ניתוב דינמית. הבעיה
מתפרקת לשתיים בלתי תלויות: סידור בתוך אצווה, והרכבת האצווה.

אם מישהו מציע re-optimization תוך כדי נסיעה — זה סותר את האילוץ הזה.

---

## פונקציית המטרה — אל תחליף אותה ב-TSP

זו **לא** בעיית הסוכן הנוסע. TSP ממזער אורך מסלול. ללקוח לא אכפת
כמה השליח נסע, אכפת לו מתי הוא קיבל. ממזערים:

```
Σᵢ ( arrival_timeᵢ − ready_atᵢ )
```

זו **Minimum Latency Problem** (Traveling Repairman). המסלול האופטימלי
שלה עשוי להיות ארוך יותר במרחק מזה של TSP — וזה נכון, לא באג.

n ≤ 8 → ברוט-פורס על 40,320 תמורות, ~20ms, אופטימלי מוכח.
**אל תוסיף OR-Tools** לפני שיש משתמשים אמיתיים: זה Python, כלומר
שירות נפרד לפרוס ולתחזק. מעל n=10 → Held-Karp.

### מאיפה מגיעים זמני הנסיעה — ואל תשכור שרת בשבילם

**אל תתחיל את שלב 6 בהתקנת OSRM על VPS.** האפיון המקורי הניח זאת,
וזה נדחה: מסעדה אחת היא **~25 חישובי מסלול ביום**, וזה נכנס בנוחות
בשכבה החינמית של שירות מטריצה מתארח (OpenRouteService — אותם נתוני
OSM, אותו רישיון, אפס התנגשות עם Leaflet). ה-$6 לחודש אינם המחיר
האמיתי של VPS; המחיר הוא התקנה, מפה שמזדקנת, עדכוני אבטחה ומישהו
שיבחין שהשרת נפל בשעת עומס — בפרויקט של אדם אחד שאינו מפתח, זה הסעיף
היקר. OSRM עצמי חוזר לדיון בסביבות 30–50 מסעדות, ולא לפני.

**מרחק אווירי אינו תחליף.** 400 מטר אוויריים יכולים להיות שבע דקות
נסיעה כשביניהם מסילה או כביש מהיר. זה מטשטש בדיוק את ההבחנה שהאלגוריתם
חי ממנה — אותו כשל של קואורדינטה שגויה בשקט, בשכבה אחרת.

**בודדו את הספק** מאחורי `getDurationMatrix(points)`, כפי ש-
`addressSearch.ts` מבודד את ספקי הכתובות. האלגוריתם לא צריך לדעת
מאיפה הגיעו המספרים. ראו `spec.md` §3.2.

### שתי הצעות שכבר נדחו — אל תחזור אליהן

**"למזער את הזמן מהיציאה ועד החזרה."** זה TSP בניסוח אחר. בסיבוב סגור
כיוון הנסיעה **אינו משנה את משך הנסיעה בכלל**, אבל קובע לגמרי מי מחכה:
R ומשלוחים A (דקה), B ו-C (עשר דקות, דקה זה מזה) — שני הכיוונים לוקחים
22 דקות, וסכום ההמתנה הוא 24 מול 42. המדד הזה עיוור למה שאנחנו בונים.

**"מעגל המילטוני קצר ביותר, ואז לבחור מאיזה צד להתחיל."** לא אופטימלי.
R=(0,0), A=(1,0), B=(0,1), C=(10,10): המעגל הקצר הוא R-A-C-B-R, ושני
כיווניו נותנים סכום המתנה **43.4** — בעוד האופטימום, R→A→B→C, הוא
**19.3**. TSP דוחף את הרחוק לאמצע כדי לא לשלם פעמיים; מינימום המתנה
רוצה לגרוף את הקרובים מיד ולשלם את הנסיעה הארוכה פעם אחת, בסוף.
המעגל מוציא את התשובה הנכונה ממרחב החיפוש, ובחירת כיוון לא תחזיר אותה.

וחשוב מכל: מציאת המעגל הקצר ביותר **היא עצמה** TSP — אותן 40,320
תמורות. השיטה עולה בדיוק אותו דבר ומחזירה תשובה גרועה יותר.

בתוך אצווה נתונה `Σ ready_at` הוא קבוע, ולכן מזעור סכום זמני ההגעה
שקול למזעור סכום ההמתנה — **הסדר נקבע מזמני נסיעה בלבד.** `ready_at`
נחוץ למדידה ולהרכבת האצווה (שלב 7), לא לסידור בתוכה.

---

## החלטות ארכיטקטורה — אל תשנה בלי סיבה

| החלטה | למה |
|---|---|
| `routes` + `route_stops` כישויות נפרדות | `courier_id` ישירות על משלוח מוחק את **הסדר**, שהוא כל המוצר |
| `shifts` במקום `active`+`current_business_id` | שומר היסטוריה ומאפשר מצב "הוזמן וטרם אישר" |
| `ready_at` נכתב בכל משלוח | בלעדיו אין פונקציית מטרה ואין מה למדוד. **קריטי** |
| `price_agorot integer` | לא float. אף פעם |
| פרופיל נוצר בטריגר `handle_new_user` | מונע משתמש יתום אם קריאה שנייה נכשלת |
| `me()` RPC בעליית האפליקציה | תפקיד + פרופיל בבקשה אחת |
| חיפוש שליח = התאמה מדויקת בלבד | אין endpoint שמחזיר רשימת שליחים — עסק לא ידלה את המאגר |
| `businesses.lat/lng` nullable | אין קואורדינטות בהרשמה; גיאוקודינג מגיע בשלב 2 |
| טבלת `addresses` אצלנו, לא ספק חיצוני | ראו "כתובות" למטה. גם רישוי וגם דיוק |
| טופס משלוח פותח ב**טלפון**, לא בכתובת | לקוח חוזר = אפס הקלדה. מה שלא מוקלד לא משתבש |

---

## מוסכמות

- **עברית RTL** בכל ממשק. `dir="rtl"` על `<html>`.
- **שגיאות בעברית ואומרות מה לעשות.** `translateAuthError` ב-`packages/shared/src/auth.tsx`
  הוא המקום. אל תדליף מחרוזות של Supabase למשתמש.
- **טלפונים מנורמלים** דרך `normalizePhone` בשני הצדדים. אחרת החיפוש לא ימצא.
- **RLS על כל טבלה חדשה.** בלי יוצא מן הכלל. `my_business_ids()` היא
  `security definer` בכוונה — מונעת רקורסיה במדיניות.
- **טוקנים של עיצוב** ב-`styles/index.css`. כתום = חום האוכל (פעולה ראשית),
  כחול = אוכל שהתקרר. אל תוסיף צבעים אקראיים.
- שדות קלט בנייד ב-16px לפחות, אחרת iOS עושה זום.

---

## מצב נוכחי

**הושלם — שלב 0:** סכימה, טריגר הרשמה, RLS, מסכי התחברות והרשמה
בשתי האפליקציות, `me()`, `find_courier_by_phone`.

**הושלם — שלב 1 (משמרות):** חיפוש שליח לפי טלפון והזמנה למשמרת,
מסך אישור/דחייה אצל השליח, Realtime בשני הכיוונים, רשימות
"במשמרת" ו"ממתינים לאישור" בדשבורד. **בדיקת הקבלה עברה** — דשבורד
במחשב מול שליח בנייד, ההזמנה הגיעה מיידית.

שני דברים שנלמדו שם ורלוונטיים לכל טבלה שתשודר בהמשך:

- **טבלה חדשה אינה בפרסום `supabase_realtime`.** מנוי עליה *מצליח*
  ולא מדווח שגיאה — הוא פשוט לא מקבל אירועים. אומת בפועל. כל טבלה
  שצריכה Realtime דורשת `alter publication` + `replica identity full`
  (בלי השנייה, אכיפת RLS על UPDATE/DELETE נופלת). ראו מיגרציה 0005.
- **`security definer` בלי `set search_path` נשבר** כשהפונקציה נקראת
  מהקשר שאינו האפליקציה — למשל טריגר על `auth.users`, שרץ אצל
  `supabase_auth_admin`. זה הפיל את ההרשמה. ראו מיגרציה 0004.
  `me()`, `find_courier_by_phone` ו-`my_business_ids()` עדיין חשופות
  לאותה חולשה; הן עובדות כי הן נקראות מהאפליקציה בלבד.

**הושלם — שלב 2 (משלוחים + גיאוקודינג):** טופס הזנה בשלוש שכבות,
מאגר כתובות מקומי, ו-`ready_at`. **בדיקת הקבלה עברה** — משלוח אמיתי
בבאר שבע נשמר עם `geocode_status='ok'`, קואורדינטות מלאות ו-`ready_at`.

**המדד שהוגדר לשלב ועמד בו:** כל משלוח מקבל קואורדינטה, ולכל משלוח
יש `ready_at`. לא "כמה אחוז מהכתובות מדויקות לבניין".

### כתובות — למה מאגר מקומי ולא ספק

כל האפשרויות נבדקו מול כתובות אמיתיות, לא לפי מוניטין:

- **גוגל פסולה** — לא בגלל מטמון של 30 יום (זה לא מגביל אותנו; הקואורדינטה
  נצרכת בחישוב המסלול, שעות אחרי ההזנה). האיסור שכן חוסם: אסור לצרוך
  תוכן גיאוקודינג של גוגל **יחד עם מפה שאינה שלה**. Leaflet פסול. זו
  החלטת סטאק, לא החלטת ספק — או הכל גוגל, או הכל פתוח.
- **Nominatim הציבורי** אוסר השלמה אוטומטית במפורש.
- **GovMap** אינו מחזיר קואורדינטות כלל — הוא כלי גוש/חלקה.
- **OSM/Photon** חזק בגוש דן, חלש בפריפריה. נמדד: "היילפרין ליפמן 18"
  לא נמצא כלל; "שדרות רגר" חזר כנקודה אחת — כשהרחוב **2,889 מטר**.

מכאן `addresses` (מיגרציות 0007–0009) — מאגר ישראלי פתוח, CC-BY,
טעון אצלנו. אין שירות נוסף לפרוס, אין מגבלת קצב, אין התנגשות רישוי.
Photon נשאר גיבוי לערים שלא נטענו. `scripts/build_addresses_csv.py`
ממיר מ-EPSG:2039 ל-WGS84 ומפרק את עמודת מספר הבית הלא-אחידה.

**חובות פתוחים:** ייחוס CC-BY במסך כלשהו לפני יציאה לשוק. המאגר הוא
צילום מנובמבר 2024 ויזדקן. נטענה **באר שבע בלבד** (20,088 שורות),
ובה 18% מהכתובות במקור בלי קואורדינטות — אותן יפתור תיקון ידני בשלב 3.

### דיוק — כמה נדרש, ולמה

קואורדינטה שגויה בשקט **גרועה מהיעדר קואורדינטה**: היא נראית תקינה עד
שהשליח עומד במקום הלא נכון. לכן הצעה ברמת רחוב נשמרת כ-`manual`
ולא כ-`ok`, ומסומנת למשתמש ברגע הבחירה.

זה קריטי דווקא לאלגוריתם שלנו: מינימום המתנה מסדר לפי **מי קרוב ומי
רחוק**. קלט שמטשטש את ההבחנה הזו יביא להחלטה שגויה בביטחון מלא.

**הושלם — שלב 3 (מפה):** Leaflet בלשונית נפרדת, המסעדה והמשלוחים
הפתוחים כסיכות, ו**נעיצה ידנית** למשלוח בלי מיקום. **בדיקת הקבלה
עברה** — כתובת מומצאת נכנסה בלי קואורדינטות, נועצה על המפה, ונשמרה
עם `geocode_status='manual'`.

- `leaflet` בלבד, **בלי `react-leaflet`** — מפה, סיכות וקליק לא
  מצדיקים עטיפה נוספת שרגישה לגרסאות React.
- סמלי ברירת המחדל של Leaflet הוחלפו ב-`divIcon`: נתיבי התמונות
  שלהם נשברים תחת bundler, וכך גם אפשר לקודד משמעות בצבע.
- משמעויות `geocode_status` מופרדות: `ok` מדויק מהמאגר, `pending`
  מרכז רחוב הממתין לאדם, `manual` אדם נעץ, `failed` אין נקודה.
- מונה על הלשונית — משלוח בלי מיקום נעלם מהעין ואז מהמסלול.

**מעקב שליח חי הוסר מהמוצר — זו החלטה, לא השמטה.** האפיון המקורי
תכנן לשלב הזה סיכת שליח נעה, דיווח מיקום כל 10–15 שניות ובדיקת קבלה
של "נסיעה אמיתית ברחוב". זה לא נבנה, ולא ייבנה: **האצווה סגורה**, ולכן
מיקום השליח בזמן אמת אינו קלט לאף החלטה — אין ניתוב מחדש ואי אפשר
להוסיף עצירה. מולו עומדות סוללת השליח, המגבלה של PWA ב-iOS (שהופכת
בזכות זה ללא-רלוונטית), ותשומת לב שנגרעת מהזנת הנתונים. אם הפיילוט
יראה שבעל העסק שואל "איפה השליח" כמה פעמים ביום — זה חוזר לדיון.
נשארו מזה שלוש עמודות מתות: `couriers.last_lat/last_lng/last_location_at`.
אף שורת קוד אינה כותבת אליהן. `postgis` דומה — מותקן ב-0001 ולא בשימוש.

**חוב פתוח נוסף לפני יציאה לשוק:** אריחי OpenStreetMap הם שירות
ציבורי עם מדיניות שימוש — הייחוס מוצג, אך אינו מיועד לעומסים.
מצטרף לייחוס ה-CC-BY של מאגר הכתובות.

**הושלם — שלב 4 (מסלולים ידניים):** בעל העסק בוחר משלוחים, משייך
לשליח, מסדר עצירות ומשגר; השליח רואה את המסלול בסדר שנקבע ומסמן
מסירות. **בדיקת הקבלה עברה** — שני מסלולים רצו מקצה לקצה.

- **מסלול קופא ברגע ששולח.** עריכה רק ב-`draft`. נובע ישירות
  מהאילוץ העסקי: אצווה שיצאה סגורה, ואין ניתוב מחדש תוך כדי נסיעה.
- `picked_up_at` אחיד לכל האצווה — השליח לקח את כולם באותו רגע.
- **סגירת מסלול היא פעולת בעל העסק.** לשליח אין policy לעדכן
  `routes` (ראו 0002), ולא נוספה אחת.
- `route_stops` לא בפרסום השידור: אין בה עמודת שיוך לסינון מנוי.
  לא נדרש — המסלול קופא לפני שהשליח רואה אותו.

**לקח Realtime שלישי, וגרסה חדשה שלו:** מנוי על טבלה **אינו** מקבל
אירוע כשטבלה מקושרת משתנה. ספירת המסירות בכרטיס המסלול נתקעה כי
היא נקראה מעותק משובץ בשאילתת `routes`, בעוד השליח עדכן `deliveries`.
התיקון לא היה מנוי נוסף אלא **הסרת העותק השני**. אל תחזיקו את אותו
נתון בשני מקומות; החזיקו מקור אחד חי.

**פונקציית המטרה מדידה מהיום.** `ready_at` ו-`delivered_at` נכתבים
שניהם על נתונים אמיתיים, ולכן `Σ(delivered_at − ready_at)` הוא מספר
ולא נוסחה. בלי הצמד הזה שלב 6 לא היה יכול להוכיח שהוא עוזר.

---

## מטרת הפרויקט השתנתה — קראו את זה לפני שמתכננים

עד 08/2026 הפרויקט נוהל כמיזם מסחרי, וסדר השלבים נגזר מסיכון עסקי.
**המטרה המיידית היום היא תיק עבודות לראיונות עבודה.** בעל הפרויקט
עדיין מתכוון לדחוף אותו מסחרית בהמשך, אבל מה שנבנה עכשיו נבנה כדי
שיוצג למראיין.

**שלב 5 (פיילוט) חסום על מציאת מסעדה, והחסם אינו טכני.**

**מכאן: שלב 6 מקדים את שלב 5.** זו הפרה מכוונת של הכלל המקורי, ולא
שכחה. הנימוק לסדר 5→6 היה מסחרי — אין טעם לבנות מנוע לרכב שאיש לא
נוהג בו. בתיק עבודות הסיכון הפוך: **שלב 6 הוא כל התוכן ההנדסי של
הפרויקט.** בלי האופטימיזר זה מוצר CRUD עם מפה; אִתו זו עבודה על
Minimum Latency Problem עם פונקציית מטרה מנומקת ותוצאה מדידה.

**מה נשאר בתוקף מלא:** האילוץ העסקי, פונקציית המטרה, וכל ההחלטות
בטבלה למעלה. השתנה **סדר** הבנייה, לא מה שנכון לבנות.

**מה שאסור:** להציג נתונים מדומים כאילו היו פיילוט אמיתי. מראיין ישאל
שאלת המשך אחת וזה ייפול. הניסוח הנכון הוא "לא השגתי מסעדה, לכן מדדתי
על מערך מדומה מכתובות אמיתיות, וזה מה שהוא כן ולא מוכיח". כל מקום
שמציג נתוני דמו חייב לומר שהם מדומים.

**סדר העבודה החדש:** דמו ופריסה ← מערך מדומה למדידה ← שלב 6 עם
בדיקות ← README באנגלית.

**נבנה — שלב 6 (אופטימיזציה):** `optimize.ts` טהור ומכוסה בבדיקות,
`durationMatrix.ts` מבודד את הספק, Edge Function `duration-matrix`
מסתירה את מפתח ORS, וכפתור "חשב סדר אופטימלי" בכרטיס מסלול בטיוטה.

**שינוי מול האפיון — ה-Edge Function דקה בכוונה.** האפיון תכנן
`optimize-route` שמריצה גם את האלגוריתם, בנימוק "חישוב שאסור לתת
ללקוח לזייף". **הנימוק אינו תקף:** בעל העסק כבר מסדר עצירות ידנית
(`RouteCard.swap`) וה-RLS מתיר לו. נשאר נימוק אחד — הסתרת המפתח —
ולכן הפונקציה מספקת מטריצה בלבד, והאלגוריתם רץ בקליינט מתוך המודול
הבדוק. אחרת היה צריך עותק שני של פונקציית המטרה ב-Deno.

**מה שנמדד לפני שנבנה, ושינה את הטענה:** מול FIFO — מה שהמוצר עשה
עד היום — האופטימיזר חוסך **24%** מזמן ההמתנה. מול שכן-קרוב הוא חוסך
**0.9%**, והחציון אפס. הסיבה היא גודל האצווה: שכן-קרוב אופטימלי
ב-94% מאצוות של 3 עצירות ורק ב-43% של 8. **אל תנסחו את ההישג כניצחון
על אינטואיציה אנושית** — הוא ניצחון על סדר ההכנה. ראו `docs/baseline.md`.

מכאן גם: שכן-קרוב הוא נפילה רכה מצוינת אם ORS אינו זמין. `getDurationMatrix`
נופל להערכת מרחק אווירי **ומסמן זאת במסך** — הערכה שמוצגת כנתון אמיתי
היא אותו כשל של קואורדינטה שגויה בשקט.

---

**נבנה — תשתית הדמו והפריסה:**

- `scripts/build_demo_seed.py` מייצר את `supabase/seed/demo_seed.sql`.
  הכתובות נבחרות מ-`data/addresses_beer_sheva.csv` — אותו מאגר
  שהאפליקציה מחפשת בו — כדי שהמרחקים יהיו אמיתיים. הבחירה
  דטרמיניסטית, כך שצילומי מסך לא מתיישנים. `npm run seed`.
- ה-SQL אידמפוטנטי ומאפס את הדמו בהרצה חוזרת. הוא מזהה את העסק לפי
  אימייל מ-`auth.users`, ולכן **אינו דורש `service_role`** ורץ
  ב-SQL editor של Supabase — עקבי עם איך שמיגרציות מורצות כאן.
- `docs/deploy.md` — מדריך העלאה מלא, צעד אחר צעד.
- `vercel.json` בכל אפליקציה. שני פרויקטים נפרדים מאותו ריפו.

**מלכודת שתחזור:** משתני `VITE_` נצרבים בזמן הבנייה. שינוי משתנה
ב-Vercel לא משפיע עד `Redeploy`.

**19 שורות במאגר באר שבע יושבות במרכז הארץ** — רחובות ששמם קיים בכמה
ערים ("נעמי שמר", "יפה ירקוני") שקיבלו עיר שגויה במקור, חלקם עם
שכונות של ראש העין. 0.09% מהשורות, אבל קואורדינטה במרחק 100 ק״מ
הורסת מסלול שלם. הסקריפט מסנן בתיבה תוחמת; **המאגר עצמו טרם נוקה.**

**נסגרו:**

- **הקשחת `security definer`** (0011). כל חמש הפונקציות במערכת
  מקובעות ל-`search_path=public,pg_temp`. אומת מול `pg_proc`.
  טבלה או פונקציה חדשה חייבת להיוולד ככה.
- **ייחוס** — `Credits` ב-`packages/shared`, מוצג בשתי האפליקציות.
  הייחוס של Leaflet לא מספיק: הוא מכסה אריחים בלבד, ורק במסך שיש
  בו מפה. כתובות מוצגות גם אצל השליח.

**נותר:** טעינת ערים מעבר לבאר שבע. הקבצים מוכנים ב-`data/israel`
(תשעה חלקים, 348,678 שורות, ממוינים מהעיר הגדולה לקטנה), הייבוא
עצמו טרם בוצע. **לבדוק את גודל המסד אחרי שלושה חלקים** — האינדקס
על מאות אלפי שורות תופח, והמכסה החינמית 500MB.

---

## סדר השלבים — אל תדלג

> **הסדר הזה תקף לשלבים 0-4, שהושלמו. מכאן ואילך ראו "מטרת הפרויקט
> השתנתה" למעלה — שלב 6 מקדים את שלב 5, כי הפיילוט חסום חיצונית.**

0 תשתית → 1 משמרות → 2 משלוחים+גיאוקודינג → 3 מפה →
4 מסלולים ידניים → **5 פיילוט שבועיים בלי קוד חדש** →
6 אופטימיזציה → 7 המלצות אצווה

שלבים 1-4 הם מוצר עובד בלי שורת אופטימיזציה. זה מכוון: אם בעל
העסק לא מקבץ משלוחים ידנית בשלב 4, שלב 6 חסר משמעות.

**הסיכון האמיתי הוא הזנת נתונים, לא האלגוריתם.** אם בעל העסק צריך
להקליד 8 כתובות ידנית, המערכת מתה. כל רעיון שמקצר את זה עדיף על
שיפור באחוזים במסלול.

---

## פקודות

```bash
npm install
npm run dashboard     # 5173
npm run courier       # 5174 (host: true — לבדיקה מנייד באותה רשת)
supabase db push      # הרצת מיגרציות
npm run typecheck
```

`.env` בכל אפליקציה: `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`.
בפיתוח: כבה Confirm email ב-Authentication → Providers.
**`service_role` key לעולם לא בקוד לקוח.**

---

## בקשות ממני

- לפני שינוי סכימה — מיגרציה חדשה, לא עריכה של קיימת.
- טבלה חדשה נולדת עם RLS באותו commit.
- אל תוסיף ספרייה בלי להגיד למה הקיים לא מספיק.
- כשמשהו סותר את האילוץ העסקי או את פונקציית המטרה — תעצור ותגיד,
  אל תתאים את הקוד בשקט.
