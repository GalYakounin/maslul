# מערכת ניהול וייעול משלוחים — אפיון ותוכנית מימוש

> מסמך תכנון טכני. **גרסה 2.0** — סונכרן מול הקוד שנבנה בפועל.
>
> גרסה 1.0 נכתבה לפני המימוש ותיארה החלטות שנבדקו ונדחו בדרך (גיאוקודינג
> של גוגל, Edge Functions לגיאוקודינג, מעקב שליח חי). הגרסה הזו מתקנת אותן.
>
> **כלל קריאה:** כל סעיף מסומן ✅ **נבנה** או 🔜 **מתוכנן**. מה שמסומן
> "נבנה" אפשר לסמוך עליו כתיאור של הקוד; מה שמסומן "מתוכנן" הוא כוונה
> שטרם נבדקה מול המציאות. **מקור האמת הסופי הוא המיגרציות** ב-
> `supabase/migrations` — כשיש סתירה, הן צודקות והמסמך הזה מיושן.
>
> נימוקים תמציתיים ומצב נוכחי מפורט: `CLAUDE.md` בשורש.

---

## 1. מה המערכת עושה

מערכת SaaS למסעדות שמפעילות שליחים משלהן. שני סוגי משתמשים:

- **בעל עסק** — לוח בקרה בדפדפן: הזנת משלוחים, ניהול שליחים במשמרת, מפה, קיבוץ משלוחים למסלול והצמדתו לשליח.
- **שליח** — אפליקציה מינימלית בנייד: לאיזו מסעדה הוא משויך, מה המסלול שלו, וסימון מסירה.

**האילוץ העסקי המרכזי** (שאומת מול בתי עסק): שליח שיצא למסלול אינו חוזר לאסוף משלוח נוסף באמצע. כל יציאה היא **אצווה סגורה**. זה מפשט את הבעיה מבעיה דינמית לשתי בעיות סטטיות נפרדות.

---

## 2. ארכיטקטורה כללית

```
┌─────────────────────┐         ┌─────────────────────┐
│  דשבורד בעל עסק      │         │  אפליקציית שליח      │
│  React + Vite       │         │  React + Vite (PWA) │
│  Leaflet (מפה)      │         │                     │
└──────────┬──────────┘         └──────────┬──────────┘
           │                                │
           │    Supabase JS Client          │
           │    (REST + Realtime WS)        │
           └────────────┬───────────────────┘
                        │
           ┌────────────▼────────────────┐
           │        SUPABASE             │
           │  ┌───────────────────────┐  │
           │  │ Auth (JWT)            │  │
           │  ├───────────────────────┤  │
           │  │ Postgres              │  │
           │  │ + Row Level Security  │  │
           │  │ + pg_trgm             │  │
           │  ├───────────────────────┤  │
           │  │ Realtime (WebSocket)  │  │
           │  ├───────────────────────┤  │
           │  │ טבלת addresses         │  │
           │  │ (מאגר ישראלי טעון)      │  │
           │  └───────────────────────┘  │
           └──────┬───────────────┬──────┘
                  │               │
        ┌─────────▼──────┐  ┌─────▼────────────┐
        │ Photon (OSM)   │  │ OpenRouteService │
        │ גיבוי לכתובות   │  │ מטריצת זמנים      │
        │ ✅ נבנה         │  │ ✅ נבנה           │
        └────────────────┘  └──────────────────┘

שני השירותים החיצוניים נשענים על נתוני OpenStreetMap, ולכן חולקים
רישיון אחד (ODbL) ואותו ייחוס — ואין התנגשות עם מפת Leaflet. זה לא
צירוף מקרים אלא אילוץ מנחה: ברגע שנכנס ספק מעולם אחר, הוא גורר איתו
את כל הסטאק (§3.3).
```

**עקרון מנחה:** רוב הלוגיקה יושבת ב-DB (אילוצים + RLS), לא בקוד הלקוח.

**Edge Function אחת במערכת, ולא שתיים.** גרסה 1.0 תכננה `geocode`
ו-`optimize-route`.

הראשונה התייתרה: הגיאוקודינג הוא חיפוש בטבלה מקומית דרך RPC רגיל
(`search_addresses`), ואין ספק חיצוני שצריך להסתיר מפתח עבורו.

השנייה **הצטמצמה** ל-`duration-matrix`, שמספקת מטריצת זמנים בלבד.
גרסה 1.0 נימקה אותה בשני טעמים — הסתרת מפתח, ו"חישוב שאסור לתת ללקוח
לזייף". **הנימוק השני אינו תקף:** בעל העסק כבר מסדר את העצירות ידנית
(`RouteCard.swap`), וה-RLS מתיר לו זאת במכוון; אין מה להגן עליו. נשאר
רק המפתח, ולכן הפונקציה דקה והאלגוריתם רץ בקליינט מתוך
`packages/shared/src/optimize.ts` — המודול שמכוסה בבדיקות. החלופה
הייתה עותק שני של פונקציית המטרה ב-Deno, וזה בדיוק הכשל שהפיל את
ספירת המסירות בשלב 4 (§7.1).

---

## 3. סטאק טכנולוגי

| רכיב | בחירה | מצב | נימוק |
|---|---|---|---|
| שפה | TypeScript (Full-stack) | ✅ | טיפוסים משותפים בין שרת ללקוח מונעים באגי חוזה |
| בסיס נתונים | Postgres 15 | ✅ | הנתונים רלציוניים לחלוטין |
| Backend | Supabase | ✅ | Auth + Realtime + RLS מהקופסה. חוסך ~3 שבועות תשתית |
| חיפוש טקסט | `pg_trgm` | ✅ | `word_similarity` להשלמת כתובות. ראו §3.3 |
| דשבורד | React 18 + Vite + TailwindCSS | ✅ | |
| מפה | Leaflet + אריחי OpenStreetMap | ✅ | חינם. **בלי `react-leaflet`** — ראו §3.4 |
| אפליקציית שליח | PWA | ✅ | ראו §3.1 |
| גיאוקודינג | טבלת `addresses` מקומית + Photon כגיבוי | ✅ | ראו §3.3. **לא גוגל** |
| מטריצת זמנים | OpenRouteService (מתארח) | ✅ | שכבה חינמית, אפס תשתית. ראו §3.2 |
| מטריצת זמנים בקנה מידה | OSRM עצמי על VPS | 🔜 עתידי, בתנאי | רק כשעוברים את השכבה החינמית. ראו §3.2 |
| לוגיקת שרת חיצונית | Edge Function `duration-matrix` | ✅ | דקה בכוונה — מסתירה מפתח בלבד. ראו §2 |
| ניהול state | React hooks בלבד | ✅ | ראו §3.5 |

### 3.1 אפליקציית השליח — PWA, ומה זה עולה

**iOS אינו מאפשר ל-PWA לדווח מיקום ברקע.** ברגע שהמסך ננעל, זרם המיקום נפסק. אין מסביב לזה דרך.

בגרסה 1.0 זה נחשב מגבלה שדורשת מעבר ל-Expo בהמשך. **בפועל זה כבר לא
רלוונטי** — מעקב מיקום חי אינו חלק מהמוצר (ראו §7.2). מה שהשליח כן צריך
לעשות בנייד — לראות מסלול קפוא ולסמן מסירה — עובד ב-PWA בלי הסתייגות.

מעבר ל-Expo יישקל רק אם יעלה צורך אמיתי בהתראות דחיפה כשהאפליקציה סגורה.

### 3.2 מטריצת זמנים — שירות מתארח תחילה, שרת עצמי רק בתנאי

שלב 6 צריך תשובה לשאלה אחת: **כמה דקות נסיעה בין כל שתי נקודות
באצווה.** בלי המספר הזה אין מה למזער — האלגוריתם יודע לסדר, אבל אין לו
לפי מה.

**מרחק אווירי נפסל.** קו ישר הוא חינם ורץ בדפדפן, אבל הוא מתעלם
מכבישים: שני בתים במרחק 400 מטר אוויריים יכולים להיות שבע דקות נסיעה
אם ביניהם מסילה, כביש מהיר או שכונה שסגורה מצד אחד. זו **בדיוק
ההבחנה שהאלגוריתם חי ממנה** — מי קרוב ומי רחוק. זה אותו כשל של
קואורדינטה שגויה בשקט (§3.3), רק בשכבה אחרת: התשובה נראית סבירה
ומסדרת לא נכון.

**גוגל נפסלה** מאותה סיבה כמו בגיאוקודינג — היא גוררת את כל הסטאק
(§3.3). בנוסף, Distance Matrix מחייב **לפי תא**: 6 נקודות = 36 תאים
לכל חישוב.

נותרו שתי דרכים לקבל מטריצה אמיתית מנתוני OSM, והן נבדלות בעיקר
בשאלה מי מתחזק את המכונה.

#### הבחירה: OpenRouteService, שירות מתארח ✅ ההחלטה

שולחים רשימת נקודות בבקשת HTTP ומקבלים מטריצת זמנים. אותם נתוני OSM,
אותו רישיון ODbL, אפס התנגשות עם Leaflet. **אין מה לפרוס ואין מה
לתחזק.**

הצריכה שלנו: מסעדה אחת, 100 משלוחים ביום, אצוות של ארבעה ⇐
**כ־25 חישובי מסלול ביום**.

**המכסה בפועל — נקראה מלוח הבקרה של החשבון, 25/08/2026:**

| Matrix V2 | |
|---|---|
| ליום | **500** |
| לדקה | 40 |

> **תיקון.** גרסה קודמת של המסמך רשמה כאן 2,500 ליום ו־60 בדקה, על
> סמך מקור משני. הלוח של HeiGIT אומר 500 ו־40, והוא הסמכות. המספר
> הנכון קטן פי חמישה — ולכן גם הסף שבו OSRM עצמי מוצדק נמוך יותר
> ממה שהמסמך טען. אל תסמכו כאן על מקור שאינו החשבון עצמו.

מגבלת הגודל אינה רלוונטית: ORS מתיר עד 3,500 צמדי מקור×יעד בבקשה
אחת, ואצווה של 8 עצירות + המסעדה היא 9×9 = 81.

#### השדרוג: OSRM עצמי על VPS 🔜 בתנאי

**VPS** הוא מחשב שכור — חתיכה משרת בחוות שרתים, עם לינוקס וכתובת
קבועה, כ־$6 לחודש. עליו רץ OSRM: מורידים את מפת ישראל (~200MB), הוא
לועס אותה פעם אחת ומחזיק אותה **בזיכרון**, ומשם עונה במילישניות.

המילים "מחזיק בזיכרון" הן הסיבה שזה דורש VPS ולא Supabase או Vercel:
זהו תהליך שצריך לרוץ ברציפות. הטעינה ארוכה מהתשובה, ולכן אי אפשר
להעיר אותו לשנייה ולכבות — הוא לא מתאים לארכיטקטורה שמתעוררת, עונה
ונעלמת.

```bash
wget https://download.geofabrik.de/asia/israel-and-palestine-latest.osm.pbf
docker run -t -v "${PWD}:/data" ghcr.io/project-osrm/osrm-backend \
  osrm-extract -p /opt/motorcycle.lua /data/israel-and-palestine-latest.osm.pbf
docker run -t -v "${PWD}:/data" ghcr.io/project-osrm/osrm-backend osrm-partition /data/israel-and-palestine-latest.osrm
docker run -t -v "${PWD}:/data" ghcr.io/project-osrm/osrm-backend osrm-customize /data/israel-and-palestine-latest.osrm
docker run -t -i -p 5000:5000 -v "${PWD}:/data" ghcr.io/project-osrm/osrm-backend \
  osrm-routed --algorithm mld /data/israel-and-palestine-latest.osrm
```

**התנאי למעבר, במפורש:** 500 קריאות ביום ÷ ~25 למסעדה ⇐ התקרה
נפגשת סביב **20 מסעדות פעילות**. שם $6 בחודש מכסים את כולן יחד,
וההפרש מצדיק את התחזוקה.

(המסמך טען קודם 30–50. זה נגזר מהמכסה השגויה שתוקנה למעלה.)

**עד אז זו הוצאה מיותרת**, וחשוב מכך — **מחיר התחזוקה אינו ה־$6**. הוא
התקנת לינוקס ו־Docker, מפה שמזדקנת וצריכה בנייה מחדש כשנפתח כביש,
עדכוני אבטחה למכונה חשופה לאינטרנט, ומישהו שיבחין שהיא נפלה בשעת
עומס. בפרויקט של אדם אחד שאינו מפתח, זה הסעיף היקר — לא הכסף.

**מגבלה משותפת לשתי הדרכים:** אין תנועה בזמן אמת. למשלוחי אוכל
באופנוע בתוך עיר — סביר לחלוטין.

#### מה זה אומר על המימוש

`optimize-route` חייבת לבודד את ספק המטריצה מאחורי פונקציה אחת
(`getDurationMatrix(points)`), בדיוק כפי ש־`addressSearch.ts` מבודד את
ספקי הכתובות. המעבר מ־ORS ל־OSRM צריך להיות החלפת מימוש אחד — לא
נגיעה באלגוריתם, שאינו יודע ואינו צריך לדעת מאיפה הגיעו המספרים.

### 3.3 גיאוקודינג — למה מאגר מקומי ולא ספק ✅

גרסה 1.0 קבעה "Google Geocoding API — הכי מדויק בישראל בפער ניכר".
זה נבדק מול כתובות אמיתיות ונדחה. כל האפשרויות נבחנו לפי מדידה, לא
לפי מוניטין:

- **גוגל פסולה** — לא בגלל מגבלת המטמון של 30 יום (היא לא מגבילה אותנו;
  הקואורדינטה נצרכת בחישוב המסלול, שעות אחרי ההזנה). האיסור שכן חוסם:
  אסור לצרוך תוכן גיאוקודינג של גוגל **יחד עם מפה שאינה שלה**. Leaflet
  פסול. זו החלטת סטאק, לא החלטת ספק — או הכל גוגל, או הכל פתוח.
- **Nominatim הציבורי** אוסר השלמה אוטומטית במפורש.
- **GovMap** אינו מחזיר קואורדינטות כלל — הוא כלי גוש/חלקה.
- **OSM/Photon** חזק בגוש דן, חלש בפריפריה. נמדד: "היילפרין ליפמן 18"
  בבאר שבע לא נמצא כלל; "שדרות רגר" חזר כנקודה אחת — כשהרחוב
  **2,889 מטר**.

מכאן טבלת `addresses`: מאגר כתובות ישראלי פתוח (odata.org.il, CC-BY),
מומר מ-EPSG:2039 ל-WGS84 וטעון אצלנו. אין שירות נוסף לפרוס, אין מגבלת
קצב, אין התנגשות רישוי. Photon נשאר גיבוי לערים שטרם נטענו.

**דיוק — כמה נדרש, ולמה.** קואורדינטה שגויה בשקט **גרועה מהיעדר
קואורדינטה**: היא נראית תקינה עד שהשליח עומד במקום הלא נכון. לכן הצעה
ברמת רחוב (`precise: false` מ-Photon) נשמרת כ-`manual` ולא כ-`ok`,
ומסומנת למשתמש ברגע הבחירה. זה קריטי דווקא לאלגוריתם שלנו: מינימום
המתנה מסדר לפי **מי קרוב ומי רחוק**, וקלט שמטשטש את ההבחנה הזו יביא
להחלטה שגויה בביטחון מלא.

### 3.4 Leaflet בלי `react-leaflet` ✅

מפה, סיכות וקליק לא מצדיקים עטיפה נוספת שרגישה לגרסאות React. בנוסף,
סמלי ברירת המחדל של Leaflet הוחלפו ב-`divIcon`: נתיבי התמונות שלהם
נשברים תחת bundler, וכך גם אפשר לקודד משמעות בצבע.

### 3.5 בלי TanStack Query ובלי Zustand ✅

גרסה 1.0 תכננה את שניהם. בפועל ה-state של המוצר הוא ארבע רשימות
(משלוחים, מסלולים, משמרות, פרופיל), כל אחת עם hook ייעודי ב-
`packages/shared` שמנוי ל-Realtime. אין cache לנהל ואין state גלובלי
לשתף בין מסכים מרוחקים. הכלל מ-`CLAUDE.md` חל: ספרייה נכנסת רק כשנאמר
במפורש למה הקיים לא מספיק.

---

## 4. מודל נתונים ✅

### 4.1 סכימת הליבה (מיגרציה 0001)

```sql
create extension if not exists pgcrypto;

-- ═══════════════ עסקים ═══════════════
create table businesses (
  business_id   uuid primary key default gen_random_uuid(),
  name          text not null,
  address       text not null,
  lat           double precision,          -- nullable: אין נ״צ בהרשמה
  lng           double precision,
  phone         text,
  -- הגדרות אצווה שבעל העסק שולט בהן (בשימוש משלב 7)
  batch_max_size          int not null default 4,
  batch_max_wait_minutes  int not null default 8,
  created_at    timestamptz not null default now()
);

-- קישור משתמשי Auth לעסק (מאפשר כמה מנהלים לעסק אחד)
create table business_members (
  user_id      uuid not null references auth.users(id) on delete cascade,
  business_id  uuid not null references businesses(business_id) on delete cascade,
  role         text not null default 'owner' check (role in ('owner','dispatcher')),
  primary key (user_id, business_id)
);

-- ═══════════════ שליחים ═══════════════
create table couriers (
  courier_id        uuid primary key references auth.users(id) on delete cascade,
  name              text not null,
  phone             text not null,
  vehicle_type      text not null default 'motorcycle'
                    check (vehicle_type in ('motorcycle','scooter','car','bicycle')),
  last_lat          double precision,      -- ראו §4.3
  last_lng          double precision,      -- ראו §4.3
  last_location_at  timestamptz,           -- ראו §4.3
  created_at        timestamptz not null default now()
);

-- ═══════════════ משמרות ═══════════════
-- מחליף את active + current_business_id. שומר היסטוריה ומאפשר "ממתין לאישור".
create table shifts (
  shift_id     uuid primary key default gen_random_uuid(),
  courier_id   uuid not null references couriers(courier_id) on delete cascade,
  business_id  uuid not null references businesses(business_id) on delete cascade,
  status       text not null default 'pending'
               check (status in ('pending','active','ended','rejected')),
  invited_at   timestamptz not null default now(),
  accepted_at  timestamptz,
  ended_at     timestamptz
);

-- שליח לא יכול להיות במשמרת פעילה בשני עסקים במקביל
create unique index one_active_shift_per_courier
  on shifts (courier_id) where status = 'active';

-- ═══════════════ משלוחים ═══════════════
create table deliveries (
  delivery_id     uuid primary key default gen_random_uuid(),
  business_id     uuid not null references businesses(business_id) on delete cascade,

  order_details   text,
  customer_name   text,
  customer_phone  text not null,
  address         text not null,
  address_note    text,                    -- קומה, כניסה, קוד שער

  lat             double precision,
  lng             double precision,
  geocode_status  text not null default 'pending'
                  check (geocode_status in ('pending','ok','failed','manual')),

  price_agorot    integer not null default 0,   -- ★ אגורות, לא float
  payment_method  text not null default 'cash'
                  check (payment_method in ('cash','card_online','card_on_delivery')),
  paid            boolean not null default false,

  status          text not null default 'new'
                  check (status in ('new','ready','assigned','picked_up','delivered','cancelled')),

  -- ★ חותמות הזמן — הבסיס לכל מדידה ולכל אופטימיזציה
  created_at      timestamptz not null default now(),
  ready_at        timestamptz,   -- יצא מהמטבח
  picked_up_at    timestamptz,   -- השליח לקח
  delivered_at    timestamptz    -- נמסר ללקוח
);

-- ═══════════════ מסלולים ═══════════════
create table routes (
  route_id                    uuid primary key default gen_random_uuid(),
  business_id                 uuid not null references businesses(business_id) on delete cascade,
  courier_id                  uuid references couriers(courier_id),
  status                      text not null default 'draft'
                              check (status in ('draft','offered','dispatched','completed','cancelled')),
  estimated_duration_seconds  int,          -- נשאר null עד שלב 6
  created_at                  timestamptz not null default now(),
  dispatched_at               timestamptz,
  completed_at                timestamptz
);

create table route_stops (
  route_id     uuid not null references routes(route_id) on delete cascade,
  delivery_id  uuid not null unique references deliveries(delivery_id) on delete cascade,
  sequence     int not null,          -- ★ ידני בשלב 4, פלט האלגוריתם משלב 6
  eta          timestamptz,           -- נשאר null עד שלב 6
  primary key (route_id, delivery_id)
);
```

### 4.2 מאגר הכתובות (מיגרציות 0007–0009)

```sql
create extension if not exists pg_trgm;

create table addresses (
  address_id    bigserial primary key,
  city          text not null,
  street        text not null,
  house_number  text not null,
  neighbourhood text,
  lat           double precision not null,
  lng           double precision not null,
  search_text   text generated always as (
                  street || ' ' || house_number || ' ' || city
                ) stored
);

create index idx_addresses_search on addresses using gin (search_text gin_trgm_ops);
create index idx_addresses_city on addresses (city);
```

הטבלה נקראת רק דרך `search_addresses(p_query, p_limit)`. הפונקציה
משתמשת ב-`word_similarity` (האופרטור `<%`), שמודד כמה טוב השאילתה
מתאימה ל**קטע** מתוך היעד ולא ליעד כולו — כך "רגר 40" מוצא את
"יצחק רגר 40 באר שבע". מיגרציה 0009 מוסיפה **דירוג לפי מספר הבית**:
בלעדיו "היילפרין ליפמן 18" החזיר גם את בתים 11, 13, 14, 15 ו-19, כי
שם הרחוב הוא רוב הטקסט ומספר הבית כמעט לא משפיע על הציון. זה **דירוג
ולא סינון** במכוון: אם הוקלד בית שאינו במאגר, עדיף להראות את שכניו
מאשר רשימה ריקה — בשעת עומס אסור שהמערכת תיתקע.

**מצב הטעינה:** באר שבע בלבד (20,088 שורות), ובה 18% מהכתובות במקור
בלי קואורדינטות — אותן פותר תיקון ידני במפה. שאר הארץ מוכן לייבוא ב-
`data/israel`: תשעה חלקים, 348,678 שורות, ממוינים מהעיר הגדולה לקטנה
כך שאפשר לייבא חלקית והכיסוי יורד בהדרגה במקום להיחתך באמצע עיר.
**יש למדוד את גודל המסד אחרי שלושה חלקים** — האינדקס על מאות אלפי
שורות תופח, והמכסה החינמית 500MB.

### 4.3 הערות תכן

| החלטה | נימוק |
|---|---|
| `price_agorot integer` | חשבון עשרוני בבינארי שבור. `0.1 + 0.2 ≠ 0.3` |
| `delivery_id unique` ב-`route_stops` | משלוח שייך למסלול אחד לכל היותר. ביטול מסלול מוחק את השורות ומשחרר את המשלוחים |
| `courier_id` על `couriers` = `auth.users.id` | חוסך JOIN בכל בדיקת הרשאה |
| `geocode_status` | כתובות בישראל נכשלות לעיתים. צריך מסלול תיקון ידני, לא כישלון שקט |
| `business_members` נפרד | מסעדה עם שני מנהלים היא מקרה רגיל |
| אין `active` על שליח | נגזר מ-`shifts`. מקור אמת יחיד |
| `businesses.lat/lng` nullable | בניגוד לגרסה 1.0: אין קואורדינטות בהרשמה, והן נקבעות במסך נפרד |

**ארבע משמעויות ל-`geocode_status`, ולא שתיים:** `ok` = מדויק מהמאגר;
`pending` = מרכז רחוב הממתין לאדם; `manual` = אדם נעץ על המפה;
`failed` = אין נקודה. ההפרדה בין `pending` ל-`manual` היא מה שמאפשר
למונה על לשונית המפה להראות כמה משלוחים עדיין בלי מיקום אמיתי.

**עמודות ללא כותב — חוב ידוע.** `couriers.last_lat`, `last_lng`
ו-`last_location_at` נוצרו בשלב 0 עבור מעקב שליח חי שהוסר מהמוצר
(§7.2). אף שורת קוד אינה כותבת אליהן. הן נשארות בסכימה כי הסרתן דורשת
מיגרציה שאינה משנה שום התנהגות; אם מעקב מיקום לא יחזור עד שלב 7,
כדאי להסיר אותן אז.

**PostGIS הותקן ואינו בשימוש.** `create extension postgis` במיגרציה
0001 נשאר מגרסה 1.0, שתכננה חישובי מרחק ב-DB. בפועל אין במערכת אף
שימוש ב-`geography`, `geometry` או פונקציית `ST_`: זמני הנסיעה יגיעו
משירות מטריצה חיצוני (§3.2), והחיפוש הטקסטואלי מ-`pg_trgm`. אין נזק
בהשארתו.

---

## 5. אבטחה — Row Level Security ✅

הרעיון: ההרשאות נאכפות ב-DB, לא בקוד. גם אם יש באג בפרונט, אי אפשר
לקרוא נתונים של עסק אחר. **RLS מופעל על כל שבע טבלאות הליבה ועל
`addresses`** — ראו מיגרציה 0002 לנוסח המלא.

עקרונות שקובעים את כל המדיניות:

- **`my_business_ids()`** מחזירה את העסקים של המשתמש הנוכחי, והיא
  הבסיס לכמעט כל policy. היא `security definer` **בכוונה** — כדי
  למנוע רקורסיה: policy על `business_members` שקוראת ל-
  `business_members` תיתקע.
- **שליח לא רואה משלוח לפני שהמסלול הוצמד לו** (`routes.status in
  ('offered','dispatched')`). אין דליפה של רשימת הלקוחות של המסעדה.
- **שליח מעדכן משלוח רק כשהמסלול `dispatched`.** לשליח **אין** policy
  לעדכן `routes` — ולכן **סגירת מסלול היא פעולת בעל העסק**. זו החלטה,
  לא השמטה.
- **אין endpoint שמחזיר רשימה.** בעל עסק רואה פרטי שליח רק אם קיימת
  ביניהם שורת `shift`; שליח רואה עסק רק באותו תנאי. חיפוש שליח נעשה
  ב-`find_courier_by_phone` בהתאמה מדויקת בלבד — צריך לדעת את הטלפון
  מראש. הטלפון מנורמל בקליינט (`normalizePhone`) בשני הצדדים, אחרת
  החיפוש לא ימצא.
- **`addresses` קריאה לכל מחובר, כתיבה לאיש.** הטעינה היא ייבוא
  חד-פעמי, לא מהאפליקציה.

שתי הרחבות מעבר לגרסה 1.0, שבלעדיהן המערכת לא עבדה: `routes` קיבלה
policy משלה (בלי אחת, `enable rls` חוסם גישה גם מבעל העסק), ו-
`shift_courier_respond` קיבלה `with check` מפורש (בלעדיו Postgres
משתמש ב-`using` גם על השורה החדשה, וזה חסם את השליח מלשנות סטטוס
מ-`pending`).

### 5.1 הקשחת `search_path` — הלקח היקר של הפרויקט

פונקציית `security definer` מחליפה את **הרשאות** המשתמש אך **לא** את
`search_path`. שני דברים נובעים מזה:

1. **תקלה:** `handle_new_user` נשברה כשנקראה מטריגר על `auth.users`,
   שרץ בהקשר `supabase_auth_admin` — שם `public` אינו בנתיב. זה הפיל
   את ההרשמה ביום הראשון (מיגרציה 0004).
2. **חשיפה:** מי שיכול להשפיע על `search_path` של הקריאה יכול להחליף
   את הטבלאות שהפונקציה קוראת מהן, והיא תרוץ עם הרשאות הבעלים.

**כל חמש הפונקציות במערכת** — `handle_new_user`, `me`,
`find_courier_by_phone`, `my_business_ids`, `search_addresses` —
מקובעות ל-`set search_path = public, pg_temp` (0004, 0007, 0011),
ואומת מול `pg_proc`. **פונקציה חדשה חייבת להיוולד ככה.**

---

## 6. זרימות מרכזיות ✅

### 6.1 פתיחת משמרת

```
בעל עסק                      DB                        שליח
   │                          │                          │
   │─ הזמן שליח ──────────────▶│                          │
   │                    INSERT shifts                     │
   │                    status='pending'                  │
   │                          │──── Realtime push ───────▶│
   │                          │                    "הזמנה ממסעדת X"
   │                          │◀──── אישור ───────────────│
   │                    UPDATE status='active'            │
   │◀──── Realtime ───────────│                          │
   │  מופיע ברשימת הפעילים      │                          │
```

### 6.2 מהזמנה למסירה

```
1. הזנת משלוח       → הטופס נפתח ב*טלפון*: לקוח חוזר מביא איתו את
                       הכתובת והנ״צ שכבר אומתו, ואין הקלדה כלל
                     → כתובת חדשה: search_addresses() מציע השלמות
                       מהמאגר המקומי; Photon כגיבוי
                     → INSERT deliveries עם lat/lng ו-geocode_status
                       ('ok' מהמאגר, 'manual' מהצעה ברמת רחוב)

2. יצא מהמטבח       → UPDATE ready_at=now(), status='ready'   ★ קריטי

3. תיקון מיקום      → משלוח בלי נ״צ מסומן במונה על לשונית המפה
                     → בעל העסק נועץ ידנית → geocode_status='manual'

4. קיבוץ            → בעל העסק בוחר משלוחים, מסדר עצירות ידנית
                     → INSERT routes (status='draft') + route_stops

5. הצמדה ושיגור     → UPDATE routes.courier_id, status='dispatched'
                     → המסלול *קופא*: עריכה אפשרית רק ב-'draft'
                     → deliveries.picked_up_at=now() אחיד לכל האצווה
                     → Realtime push לשליח

6. מסירה            → השליח לוחץ "נמסר" בכל תחנה
                     → deliveries.delivered_at=now(), status='delivered'

7. סגירה            → *בעל העסק* סוגר → routes.status='completed'
                       (לשליח אין policy לעדכן routes — §5)
```

**שלב 4 בזרימה יוחלף בשלב 6** בקריאה ל-`optimize-route`, שתכתוב
`sequence` ו-`eta`. המבנה כבר תומך בזה: העמודות קיימות וממתינות.

---

## 7. Realtime ✅

| טבלה | בפרסום | מנוי | מה זה מפעיל |
|---|---|---|---|
| `shifts` | ✅ 0005 | שליח (`courier_id`) ודשבורד (`business_id`) | מסך אישור אצל השליח; רשימת פעילים בדשבורד |
| `deliveries` | ✅ 0006 | דשבורד (`business_id`) | רענון טבלה ומפה בין מכשירים |
| `routes` | ✅ 0010 | שליח (`courier_id`) | הצגת מסלול חדש בלי רענון |
| `route_stops` | ❌ בכוונה | — | אין בה עמודת שיוך לסינון מנוי, והמסלול קופא לפני שהשליח רואה אותו |

### 7.1 שלושה לקחים שנצרבו — קראו לפני שמוסיפים שידור

1. **טבלה חדשה אינה בפרסום `supabase_realtime`.** מנוי עליה *מצליח*
   ולא מדווח שגיאה — הוא פשוט לא מקבל אירועים. כל טבלה שצריכה
   Realtime דורשת `alter publication ... add table`.
2. **`replica identity full` חובה.** ב-UPDATE/DELETE ברירת המחדל
   משדרת רק את המפתח הראשי של השורה הישנה. אכיפת RLS על אירועי
   Realtime צריכה את שאר העמודות כדי להכריע אם המנוי רשאי לקבל את
   האירוע — בלעדיה עדכונים נבלעים בשקט.
3. **מנוי על טבלה אינו מקבל אירוע כשטבלה מקושרת משתנה.** ספירת
   המסירות בכרטיס המסלול נתקעה כי היא נקראה מעותק משובץ בשאילתת
   `routes`, בעוד השליח עדכן `deliveries`. **התיקון לא היה מנוי נוסף
   אלא הסרת העותק השני.** אל תחזיקו את אותו נתון בשני מקומות;
   החזיקו מקור אחד חי.

### 7.2 מעקב מיקום שליח — הוסר מהמוצר

גרסה 1.0 תכננה ערוץ Broadcast לדיווח מיקום כל 10–15 שניות וסיכת שליח
נעה במפה, והגדירה לשלב 3 בדיקת קבלה של "נסיעה אמיתית ברחוב".
**זה לא נבנה, ואינו מתוכנן.**

הסיבה נובעת ישירות מהאילוץ העסקי: **האצווה סגורה.** מיקום השליח בזמן
אמת אינו קלט לאף החלטה — אין ניתוב מחדש תוך כדי נסיעה, ואי אפשר להוסיף
לו עצירה. הוא נוחות לבעל העסק, לא חלק מהמוצר. מולו עומדות עלויות
ממשיות: סוללת השליח, מגבלת ה-PWA ב-iOS (§3.1), ותשומת לב שנגרעת
מהצוואר האמיתי — הזנת הנתונים (§11).

אם הפיילוט יראה שבעל העסק שואל "איפה השליח" כמה פעמים ביום, זה חוזר
לדיון. עד אז — לא. הדבר היחיד שנשאר ממנו הוא שלוש עמודות מתות ב-
`couriers` (§4.3).

---

## 8. האלגוריתם ✅

### 8.1 שתי השכבות

| שכבה | מה מחליטים | מתי |
|---|---|---|
| **א׳ — סידור** | באיזה סדר לנסוע בין נקודות האצווה | ידני בשלב 4; אוטומטי בשלב 6 |
| **ב׳ — הרכבה** | אילו משלוחים לצרף, ומתי לצאת | ידני; המלצה בשלב 7 |

### 8.2 שכבה א׳ — פונקציית המטרה

**זו לא בעיית TSP.** TSP ממזער אורך מסלול כולל. ללקוח לא אכפת כמה השליח נסע — אכפת לו מתי **הוא** קיבל.

מה שממזערים הוא **סכום זמני ההמתנה של הלקוחות**:

```
minimize   Σᵢ ( arrival_timeᵢ − ready_atᵢ )
```

זו בעיה בשם **Minimum Latency Problem** (Traveling Repairman). היא נבדלת מ-TSP: המסלול האופטימלי שלה עשוי להיות **ארוך יותר** במרחק, כי משתלם לקטוף נקודות קרובות מוקדם. מנה שהמתינה 6 דקות על הדלפק מקבלת עדיפות אוטומטית — ה-`ready_at` המוקדם מגדיל את תרומתה לפונקציה.

**הערה מתמטית שחוסכת בלבול:** בתוך אצווה נתונה `Σ ready_at` הוא קבוע,
ולכן מזעור סכום זמני ההגעה שקול למזעור סכום ההמתנה — **הסדר בתוך
האצווה נקבע מזמני נסיעה בלבד.** `ready_at` נחוץ למדידה ולהרכבת האצווה
(§8.5), לא לסידור בתוכה.

### 8.3 שתי הצעות שכבר נדחו — אל תחזרו אליהן

**"למזער את הזמן מהיציאה ועד החזרה."** זה TSP בניסוח אחר. בסיבוב סגור
כיוון הנסיעה **אינו משנה את משך הנסיעה בכלל**, אבל קובע לגמרי מי מחכה:
מסעדה R ומשלוחים A (דקה), B ו-C (עשר דקות, דקה זה מזה) — שני הכיוונים
לוקחים 22 דקות, וסכום ההמתנה הוא 24 מול 42. המדד עיוור למה שאנחנו בונים.

**"מעגל המילטוני קצר ביותר, ואז לבחור מאיזה צד להתחיל."** לא אופטימלי.
R=(0,0), A=(1,0), B=(0,1), C=(10,10): המעגל הקצר הוא R-A-C-B-R, ושני
כיווניו נותנים סכום המתנה **43.4** — בעוד האופטימום, R→A→B→C, הוא
**19.3**. TSP דוחף את הרחוק לאמצע כדי לא לשלם פעמיים; מינימום המתנה
רוצה לגרוף את הקרובים מיד ולשלם את הנסיעה הארוכה פעם אחת, בסוף. המעגל
מוציא את התשובה הנכונה ממרחב החיפוש, ובחירת כיוון לא תחזיר אותה.
וחשוב מכל: מציאת המעגל הקצר ביותר **היא עצמה** TSP — אותן 40,320
תמורות. השיטה עולה בדיוק אותו דבר ומחזירה תשובה גרועה יותר.

### 8.4 המימוש — Edge Function דקה + אלגוריתם בקליינט

```typescript
// המבנה בפועל. הקוד המלא: supabase/functions/duration-matrix/index.ts
// ו-packages/shared/src/{optimize,durationMatrix}.ts

// ═══ שכבת הבידוד מספק המטריצה ═══
// המימוש היחיד שיתחלף כשעוברים ל-OSRM עצמי (§3.2). האלגוריתם למטה
// אינו יודע ואינו צריך לדעת מאיפה הגיעו המספרים.
// המפתח יושב ב-secret של הפונקציה, לא בקוד הלקוח.
async function getDurationMatrix(points: LatLng[]): Promise<number[][]> {
  // api.openrouteservice.org בהוצאה משימוש. הנתיב החדש כולל קידומת:
  const res = await fetch('https://api.heigit.org/openrouteservice/v2/matrix/driving-car', {
    method: 'POST',
    headers: { Authorization: ORS_KEY, 'Content-Type': 'application/json' },
    // ORS מצפה ל-[lng, lat] — הפוך מהסדר המקובל. מקור טעויות שקטות
    body: JSON.stringify({
      locations: points.map(p => [p.lng, p.lat]),
      metrics: ['duration'],
    }),
  });
  const { durations } = await res.json();   // שניות. durations[0] = מהמסעדה
  return durations;
}

async function optimizeRoute(depot: LatLng, stops: Stop[]) {
  // 1. המסעדה היא אינדקס 0, העצירות 1..n
  const durations = await getDurationMatrix([depot, ...stops.map(s => s.coords)]);

  // 2. n ≤ 8 → 40,320 תמורות. ברוט-פורס, זמן ריצה ~20ms
  let best = null, bestCost = Infinity;
  for (const perm of permutations(stops.map((_, i) => i + 1))) {
    let t = 0, prev = 0, cost = 0;
    for (const idx of perm) {
      t += durations[prev][idx];
      cost += t;                             // ← סכום זמני ההגעה. ראו §8.2
      prev = idx;
    }
    if (cost < bestCost) { bestCost = cost; best = perm; }
  }
  return { order: best, cost: bestCost };
}
```

**מה קורה כששירות המטריצה לא זמין.** הוא ספק חיצוני, ובשעת עומס במסעדה
אסור שכפתור אחד שנכשל יעצור מסלול. הכפתור "חשב סדר אופטימלי" הוא
**הצעה על גבי סידור ידני שכבר עובד** — אם הקריאה נכשלת, מציגים שגיאה
בעברית והסדר הידני נשאר כפי שהוא. שלב 6 אינו מחליף את שלב 4, הוא
מתווסף לו.

**אל תגרור OR-Tools לפני שיש משתמשים.** ב-n≤8 ברוט-פורס נותן תוצאה
**אופטימלית מוכחת** במילישניות. OR-Tools הוא Python, כלומר שירות נפרד
לפרוס ולתחזק — עלות שלא מוצדקת בשלב הזה. מעל n=10, החלף ל-Held-Karp
(תכנון דינמי, O(n²·2ⁿ)).

### 8.5 שכבה ב׳ — הרכבת אצוות 🔜 שלב 7

היוריסטיקה שעובדת בפועל:

1. חלק את המפה ל-6 **סקטורים זוויתיים** של 60° סביב המסעדה.
2. משלוחים באותו סקטור הם מועמדים לאיגוד.
3. שלח כשמתקיים אחד מ: האצווה הגיעה ל-`batch_max_size`, **או** המשלוח הוותיק ביותר ממתין מעל `batch_max_wait_minutes`.
4. שני המספרים האלה כבר קיימים כעמודות על `businesses` — הם סליידרים בהגדרות העסק, לא קבועים בקוד.

**חלון ההשהיה:** השליח לא חוזר באמצע, אבל עוד לא יצא. אם מנה יוצאת מהתנור לכתובת שכבר על המסלול — שווה לעצור אותו ל-30 שניות. כפתור: *"המתן 90 שניות — מנה נוספת בכיוון שלך"*. זו הכנסה כמעט חינם.

---

## 9. תוכנית מימוש — מצב

עקרון: **שלבים 0-4 הם מוצר עובד ומכיר בלי שורת אופטימיזציה אחת.** זה
מכוון — אם בעל העסק לא מקבץ משלוחים ידנית בשלב 4, שלב 6 חסר משמעות.

### ✅ שלב 0 — תשתית
סכימה, `handle_new_user`, RLS, מסכי התחברות והרשמה בשתי האפליקציות,
`me()`, `find_courier_by_phone`.

### ✅ שלב 1 — משמרות
חיפוש שליח לפי טלפון והזמנה, מסך אישור/דחייה, Realtime בשני הכיוונים,
רשימות "במשמרת" ו"ממתינים לאישור".
**בדיקת הקבלה עברה** — דשבורד במחשב מול שליח בנייד, ההזמנה הגיעה מיידית.

### ✅ שלב 2 — משלוחים + גיאוקודינג
טופס הזנה בשלוש שכבות (טלפון → כתובת → פרטים), מאגר כתובות מקומי,
כפתור "מוכן" שכותב `ready_at`.
**בדיקת הקבלה עברה** — משלוח אמיתי בבאר שבע נשמר עם `geocode_status='ok'`,
קואורדינטות מלאות ו-`ready_at`.
**המדד שהוגדר ועמד בו:** כל משלוח מקבל קואורדינטה, ולכל משלוח יש
`ready_at`. לא "כמה אחוז מהכתובות מדויקות לבניין".

### ✅ שלב 3 — מפה
Leaflet בלשונית נפרדת, המסעדה והמשלוחים הפתוחים כסיכות, נעיצה ידנית,
מונה על הלשונית.
**בדיקת הקבלה עברה** — כתובת מומצאת נכנסה בלי נ״צ, נועצה, ונשמרה כ-`manual`.
**שינוי היקף מול גרסה 1.0:** "שליחים חיים על המפה" הוסר — ראו §7.2.

### ✅ שלב 4 — מסלולים ידניים
בחירת משלוחים, שיוך לשליח, סידור עצירות, שיגור; מסך שליח עם המסלול
בסדר שנקבע וסימון מסירה.
**בדיקת הקבלה עברה** — שני מסלולים רצו מקצה לקצה.
**כאן יש מוצר מלא. פונקציית המטרה מדידה מהיום:** `ready_at` ו-
`delivered_at` נכתבים שניהם על נתונים אמיתיים, ולכן
`Σ(delivered_at − ready_at)` הוא מספר ולא נוסחה.

### ⏸️ שלב 5 — פיילוט (שבועיים, בלי קוד חדש) — **חסום**
מסעדה אחת, שימוש יומיומי. אוספים נתונים ומודדים. תקנו רק מה ששובר.
**חסום על מציאת מסעדה** — הצוואר האמיתי של הפרויקט, והוא לא טכני.

**החסימה הזו הזיזה את סדר השלבים.** מטרת הפרויקט המיידית היא תיק
עבודות לראיונות, ולכן שלב 6 מתבצע לפניו: הוא התוכן ההנדסי של הפרויקט,
והוא אינו תלוי במסעדה. במקום הפיילוט נבנה **מערך מדומה** — אצוות
מכתובות אמיתיות בבאר שבע, שמייצר קו בסיס להשוואה.

**מה זה כן ולא מוכיח.** מערך מדומה מוכיח שהאלגוריתם משפר את פונקציית
המטרה על גיאוגרפיה אמיתית. הוא **אינו** מוכיח שבעל עסק ישתמש במערכת,
שהזנת הנתונים מהירה מספיק בשעת עומס, או שההנחות על התנהגות שליחים
נכונות. אלה נשארות פתוחות עד שיהיה פיילוט. **כל תצוגה של נתוני דמו
חייבת לומר שהם מדומים.**

### ✅ שלב 6 — אופטימיזציה
מפתח ל-OpenRouteService (בלי שרת לפרוס — §3.2), Edge Function
`optimize-route` עם בידוד הספק מאחורי `getDurationMatrix`, כפתור
"חשב סדר אופטימלי" שכותב `sequence` ו-`eta`.
**הכפתור מתווסף לסידור הידני ולא מחליף אותו** — כישלון של ספק חיצוני
לא יעצור מסלול בשעת עומס. אומת: `getDurationMatrix` נופל להערכה
ומסמן זאת במסך.

**ה-Edge Function יצאה דקה מהמתוכנן.** היא מספקת מטריצה בלבד;
האלגוריתם רץ בקליינט מתוך `optimize.ts`. הנימוק המקורי "חישוב שאסור
לתת ללקוח לזייף" אינו תקף — בעל העסק ממילא מסדר עצירות ידנית. נשאר
רק מפתח ה-API להסתיר.

**נמדד:** 24% שיפור מול FIFO, 0.9% מול שכן-קרוב. ראו `docs/baseline.md`
ואת ההסתייגויות שם.
**מדידה:** השווה `delivered_at − ready_at` לפני ואחרי. אם אין שיפור
מדיד — הבעיה בשכבה ב׳, לא בשכבה א׳.

### 🔜 שלב 7 — המלצות אצווה + אנליטיקס
סקטורים והצעת קיבוץ, דוח שבועי לבעל העסק, כפתור השהיה.

---

## 10. עלויות (מסעדה אחת, ~100 משלוחים ליום)

| שירות | היום | משלב 6 | ב-50 מסעדות |
|---|---|---|---|
| Supabase | $0 (Free) | $0 → $25 (Pro, כשעוברים 500MB) | $25 |
| מטריצת זמנים | — | **$0** (ORS, 500 קריאות ביום) | ~$6 (VPS ל-OSRM) |
| גיאוקודינג | **$0** (מאגר מקומי) | $0 | $0 |
| אירוח דשבורד | $0 | $0 | $0 |
| **סה״כ** | **$0** | **$0-25** | **~$31** |

**גרסה 1.0 הניחה VPS כבר בשלב 6. זה נדחה** — ב-25 חישובי מסלול ביום
השכבה החינמית מספיקה בסדרי גודל, וה-$6 קונים בעיקר עבודת תחזוקה
(§3.2). ה-VPS חוזר לתמונה סביב 20 מסעדות, ואז הוא מכסה את כולן
יחד — ולכן העמודה הימנית כמעט לא גדלה עם מספר הלקוחות.

גרסה 1.0 גם העריכה ~$15 לחודש לגיאוקודינג של גוגל. הסעיף הזה ירד
לאפס כשעברנו למאגר מקומי (§3.3) — וזה נשאר אפס גם ב-50 מסעדות.

הפריט שיקבע מתי עוברים ל-Pro הוא **טבלת `addresses`**, לא נפח
המשלוחים: 348,678 שורות עם אינדקס GIN מול מכסה של 500MB. ראו §4.2.

---

## 11. סיכונים

| סיכון | חומרה | מענה | מצב |
|---|---|---|---|
| **הזנה ידנית של הזמנות** — אם בעל העסק צריך להקליד 8 כתובות, לא ישתמש | 🔴 גבוה | טופס שנפתח בטלפון (לקוח חוזר = אפס הקלדה). בהמשך: הדבקה מוואטסאפ + פענוח, אינטגרציה לקופות ישראליות | מרוכך חלקית |
| **אין מסעדה לפיילוט** | 🔴 גבוה | לא טכני. חוסם את שלב 5 ואת כל מה שאחריו | **פתוח** |
| **כיסוי כתובות מחוץ לבאר שבע** | 🟠 בינוני | הקבצים מוכנים ב-`data/israel`; Photon כגיבוי בינתיים | **פתוח** |
| **גיאוקודינג שגוי בשקט** | 🟠 בינוני | הצעה ברמת רחוב נשמרת `manual` ומסומנת; נעיצה ידנית במפה | נסגר |
| **מאגר הכתובות מזדקן** — צילום מנובמבר 2024 | 🟡 נמוך | נעיצה ידנית מכסה כתובת חדשה; רענון תקופתי מהמקור | ידוע |
| **אריחי OSM הם שירות ציבורי** | 🟡 נמוך | הייחוס מוצג, אך המדיניות אינה מיועדת לעומסים. בעומס — ספק אריחים בתשלום | ידוע |
| **תמריצים הפוכים** — שליח שמשולם לפי משלוח מרוויח מאי-יעילות | 🟠 בינוני | הצג לשליח *"יותר משלוחים במשמרת"*, לא *"פחות ק״מ"* | לשלב 6 |
| **תלות בספק מטריצה חיצוני** — מכסה שנגמרת או שירות שנופל | 🟠 בינוני | הכפתור הוא הצעה על גבי סידור ידני שעובד; כישלון לא עוצר מסלול. הספק מבודד מאחורי `getDurationMatrix` (§3.2) | לשלב 6 |
| אין תנועה בזמן אמת בנתוני OSM | 🟡 נמוך | מקדם תיקון לפי שעה | לשלב 6 |

**ירדו מהרשימה:** "סוללת השליח" ו-"PWA ללא GPS ברקע ב-iOS" — שניהם היו
סיכונים של מעקב מיקום חי, שאינו חלק מהמוצר (§7.2).

---

## 12. מבנה הפרויקט ✅

```
delivery_optemizer/
├── CLAUDE.md                 # נימוקים ומצב נוכחי — קראו קודם
├── spec.md                   # המסמך הזה
├── docs/
│   └── deploy.md             # העלאה לאוויר, צעד אחר צעד
├── supabase/
│   ├── migrations/           # 0001–0011. מקור האמת לסכימה ול-RLS
│   ├── functions/            # duration-matrix — מסתירה את מפתח ORS
│   └── seed/demo_seed.sql    # נוצר אוטומטית. אל תערכו ידנית
├── packages/
│   └── shared/src/
│       ├── types.ts          # טיפוסים נגזרים מהסכימה
│       ├── auth.tsx          # useAuth + translateAuthError (עברית)
│       ├── addressSearch.ts  # כל התלות בספקי כתובות מרוכזת כאן
│       ├── optimize.ts       # פונקציית המטרה והסידור. טהור, נבדק
│       ├── durationMatrix.ts # מבודד את ספק זמני הנסיעה
│       ├── Credits.tsx       # ייחוס CC-BY ו-ODbL. חובה משפטית
│       ├── labels.ts         # תרגומי סטטוסים
│       ├── useMe / useShifts / useDeliveries / useRoutes
│       └── useRealtimeRows.ts  # המנוי הגנרי שכל ה-hooks יושבים עליו
├── apps/
│   ├── dashboard/src/        # React + Vite, פורט 5173
│   │   └── features/{auth,business,deliveries,map,routes,shifts}/
│   └── courier/src/          # PWA, פורט 5174 (host: true)
│       └── features/{auth,shifts,routes}/
├── data/
│   ├── addresses_beer_sheva.csv    # נטען
│   └── israel/addresses_01..09.csv # מוכן, טרם נטען
└── scripts/
    ├── build_addresses_csv.py     # EPSG:2039 → WGS84 + פירוק מספר בית
    ├── split_addresses_csv.py     # פיצול לחלקים שממשק Supabase מעכל
    ├── build_demo_seed.py         # נתוני דמו מכתובות אמיתיות
    ├── generate_batches.py        # מערך אצוות מדומה למדידה
    └── measure.ts                 # קו הבסיס מול האופטימום
```

**`supabase/functions/duration-matrix/`** — ה-Edge Function היחידה (§2).
היא **אינה** נפרסת עם git push; דורשת `npx supabase functions deploy`.
**אין `infra/`** — ולא נוצרה בשלב 6, כמתוכנן. היא נחוצה רק אם וכאשר
עוברים ל-OSRM עצמי (§3.2), ואז תכיל את ה-`docker-compose` שלו.
**`packages/shared`**, לא `packages/shared-types` כפי שגרסה 1.0 כתבה:
החבילה מכילה גם רכיבי React ו-hooks, לא רק טיפוסים.

---

## 13. החלטות שכדאי לא לשנות

1. **`ready_at` נכתב בכל משלוח.** בלעדיו אין מדידה, אין פונקציית מטרה,
   ואין הוכחה שהמערכת עוזרת.
2. **הכל עובר דרך `routes`.** אל תצמיד `courier_id` ישירות למשלוח —
   זה מוחק את הסדר, שהוא כל המוצר.
3. **פיילוט לפני אופטימיזציה.** שלב 5 קודם לשלב 6. הנתונים מהפיילוט
   יגידו אם השקעת במקום הנכון.
4. **מסלול קופא ברגע ששוגר.** עריכה רק ב-`draft`. נובע ישירות מהאילוץ
   העסקי — אם מישהו מציע re-optimization תוך כדי נסיעה, זה סותר אותו.
5. **RLS על כל טבלה חדשה, `search_path` מקובע על כל פונקציה חדשה.**
   בלי יוצא מן הכלל (§5.1).
6. **מיגרציה חדשה, לא עריכה של קיימת.** המיגרציות הן רשומה היסטורית
   של מה שרץ מול המסד — עריכה של אחת מהן מנתקת את הקובץ מהמציאות.
7. **תשתית לתחזק נשכרת רק כשהיא זולה מהחלופה — כולל זמן.** הפרויקט
   מנוהל בידי אדם אחד שאינו מפתח. שרת שצריך התקנה, עדכוני אבטחה
   וניטור עולה הרבה מעבר למחיר החודשי שלו. כל ספק חיצוני מבודד מאחורי
   פונקציה אחת (`addressSearch.ts` היום, `getDurationMatrix` בשלב 6),
   כדי שהמעבר לעצמי — כשיצדיק את עצמו — יהיה החלפת מימוש ולא שכתוב.
